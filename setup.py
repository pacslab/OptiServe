from setuptools import setup, find_packages

setup(
    name="Serverless-ML-Workflow-Optimizer",
    version="0.1",
    packages=find_packages(),
    package_dir={"": "."},
    # include any other necessary package metadata
)

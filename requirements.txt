# Core runtime dependencies for the optiserve library.
# Canonical source of truth is pyproject.toml — this file mirrors the [project]
# dependencies for `pip install -r requirements.txt` convenience.
# For notebook/benchmark extras use:  pip install -e ".[experiments]"
networkx==3.3
numpy==1.26.4
scipy==1.13.1
pandas==2.2.2
boto3==1.34.127
joblib==1.4.2
tqdm==4.66.4
matplotlib==3.9.0
